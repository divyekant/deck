# Deck Docker configuration
# Copy this to .env and adjust values for your setup.

# Anthropic API key (optional — only needed for session launching)
# ANTHROPIC_API_KEY=sk-ant-...

# Host directory containing your projects (mounted read-only for browsing)
# DECK_PROJECTS_DIR=~/Projects

# Port to expose Deck on
# DECK_PORT=3001
